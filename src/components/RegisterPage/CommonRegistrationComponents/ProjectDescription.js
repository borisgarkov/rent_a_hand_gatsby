import { AddProjectTextFields } from "../../AddJobs/AddProjectTextFields";
import * as React from "react";

export default function ProjectDescription({ context }) {
    return <AddProjectTextFields context={context} />
}